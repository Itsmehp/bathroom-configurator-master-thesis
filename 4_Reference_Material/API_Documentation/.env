# Backend Environment Variables
# --------------------------------
# NODE_ENV: Runtime environment (development | production | test)
NODE_ENV=development

# Server port (used in server.ts, default fallback 8000 there but validated default 3000)
PORT=8000

# JWT secret for signing auth tokens (used in auth routes & middleware)
# Generate a strong random string in production.
JWT_SECRET=vNnpqy2YVsZxNaY3N6qsRfqRFkc0msUa

# Optional: Pre-generated JWT for scripts (e.g., uploadProducts.ts) if you want to bypass manual login during dev
# WARNING: Not validated by validateEnv.ts
JWT_TOKEN=vNnpqy2YVsZxNaY3N6qsRfqRFkc0msUa


# If using docker-compose service name as host inside another container, use the service name instead of localhost.
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/myapp?schema=public
# DATABASE_URL=postgresql://postgres:professional@badeplaner.internal:5432/myapp?schema=public&sslmode=require

# Frontend origin allowed for CORS (server.ts uses FRONTEND_URL or defaults to http://localhost:3000)
FRONTEND_URL=http://localhost:3000

# MagicPlan integration keys (used in routes/magicplan.ts)
MAGICPLAN_API_KEY=247f40bbff291c30c4197f203e81f0e7d08e
MAGICPLAN_CUSTOMER_ID=5df0c613c3876

# LM Studio AI Configuration (used in services/aiService.ts)
# URL to your local LM Studio instance
LM_STUDIO_URL=http://localhost:1234/v1/chat/completions
# Model to use (must be loaded in LM Studio)
LM_STUDIO_MODEL=mistralai/mistral-7b-instruct-v0.3

# --------------------------------
